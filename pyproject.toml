[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
# The project template's own tests run inside a generated project (see test_bootstrapped_project_tests_pass).
testpaths = ["fasterapi/scaffolder/tests"]
